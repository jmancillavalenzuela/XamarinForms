<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Test.Login">
    <ContentPage.Content>
        <StackLayout>
            <Frame BackgroundColor="#2196F3" Padding="24" CornerRadius="0">
                <Label Text="Bienvenido al Sistema!" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
            </Frame>
            <Label Text="Inicio de Sesión" FontSize="Title" Padding="30,10,30,10"/>
            <Label Text="RUT" FontSize="16" Padding="30,0,30,0"/>
            <Entry x:Name="RUT" Text="" TextTransform="Uppercase" MaxLength="9" ClearButtonVisibility="WhileEditing"/>
            <Label Text="Password" FontSize="16" Padding="30,0,30,0"/>
            <Entry x:Name="Password" Text="" IsPassword="True" />
            <Button Text="Entrar"
                VerticalOptions="CenterAndExpand"
                HorizontalOptions="Center"
                 />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>